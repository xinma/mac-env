#!/bin/bash

# macbook set up - xinma
# copy vim profile

# install or update xcode
xcode-select --install

# set up tools.
brew update
brew install git telnet fping
sudo gem install iStats

# set up ssh keys
ssh-keygen -t rsa -b 4096 -C "xima@ebay.com"
# adding ssh public key to github, internal github and gitlab

# set up alias

# set up kubes

# set up minikube

# set up vagrant and virtualbox

# set up IDE
